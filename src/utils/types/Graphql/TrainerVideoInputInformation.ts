export type TrainerVideoInputInformtion = {
    title?: string;

    description?: string;

    thumbnail?: string;

    clientOnly?: boolean;

    allowComments?: boolean;

    allowLikes?: boolean;

    allowDislikes?: boolean;

    isPublic?: boolean;
}
